export const util = {
    sumario: {
        post: model => `Cadastra um ${model}`,
        findAll: model => `Busca todos os projetos ${model}`,
        findById: model => `Busca o ${model} pelo ID`,
        delete: model => `Remove o ${model} pelo ID`,
        update: model => `Atualiza o ${model} pelo ID`
    }
}